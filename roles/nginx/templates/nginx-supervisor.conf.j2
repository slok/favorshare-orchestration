[program:nginx]
;user={{ service_runner_user }}
;group={{ service_runner_group }}
command=/usr/sbin/nginx
autostart=true
autorestart=true
stderr_logfile={{ supervisord_logs_path }}/nginx_err.log
stdout_logfile={{ supervisord_logs_path }}/nginx_out.log
redirect_stderr=true
stopsignal=QUIT